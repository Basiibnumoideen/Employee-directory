# MongoDB connection string (replace with your real connection string)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# Server port (Render may override this automatically)
PORT=10000

# Allowed frontend domain (Vercel site)
CORS_ORIGIN=https://employee-directory-olive.vercel.app
